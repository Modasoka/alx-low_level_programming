#include "main.h"

/**
 * _puts - functions prints string followed by new line to stdout
 * @str: the string that is to be printed
 *
 *Return: nothing
 */

void _puts(char *str)
{
	int i = 0;


	while (*(str + i));
	{
		_putchar(*(str + i));
		i++;
	}
	_putchar('\n');
}
